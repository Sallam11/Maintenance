@model IEnumerable<Maintenance.Models.Brand_Group>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-info">Create New</a>
</p>
<table class="table table-bordered table-hover table-condensed">
    <thead class="info">

        <tr>
            
            <th>
                @Html.DisplayNameFor(model => model.BarndGroup_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BarndGroup_Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BarndGroup_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BarndGroup_Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
