@model Maintenance.Models.Model

@{
    ViewData["Title"] = "Create";
}

<h4>Create</h4>

<h4>Machine-Sub Group</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <div class="form-group">
                <label asp-for="Barnd_IDFK" class="control-label"></label>
                <select asp-for="Barnd_IDFK" class="form-control" asp-items="@(new SelectList( ViewBag.Brand_Groups, "Barnd_ID", "Barnd_Name"))"></select>
                <span asp-validation-for="Barnd_IDFK" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Model_Name" class="control-label"></label>
                <input asp-for="Model_Name" class="form-control" />
                <span asp-validation-for="Model_Name" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
